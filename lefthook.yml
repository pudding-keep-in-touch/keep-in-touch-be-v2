pre-commit:
  commands:
    biome:
      glob: '*.{js,ts,tsx,jsx}'
      run: echo "⌛️ Running Biome..." && pnpm biome check --write --no-errors-on-unmatched {staged_files}
# test 추가되면 run test

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
